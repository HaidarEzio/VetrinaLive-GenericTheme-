import React from "react";
import ShopHeader from "Components/ShopHeader";

const Layout = (props) => {
  return (
    <div>
      <ShopHeader {...props} />
    </div>
  );
};

export default Layout;
